import styled from 'styled-components';
import { TextField } from '@mui/material';

export const EditContentMultiTextField = styled(TextField)`
	fieldset{
		border: none;
	}
	.MuiInputBase-input{
		font-weight: 700;
	}
`