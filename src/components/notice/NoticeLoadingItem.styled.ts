import { Skeleton } from '@mui/material';
import styled from 'styled-components';

export const NoticeLoadingItemTitle = styled(Skeleton)`
	max-width: 200px;
`

export const NoticeLoadingItemSubTitle = styled(Skeleton)`
	max-width: 100px;
`
