import styled from 'styled-components';

export const NoticeItemTitle = styled.span`
	font-weight: 900;
	font-size: 1rem;
`;

export const NoticeItemSubTitle = styled.span`
	font-weight: 700;
  font-size: .8rem;
`