import styled from 'styled-components';
import { FormControl } from '@mui/material';

export const NoticeBibleSelect = styled(FormControl)`
	width: 150px;
	z-index: 100;
	background-color: #fff;
`

export const BibleEm = styled.em`
	font-weight: 900;
	font-size: .9rem;
	text-align: center;
`